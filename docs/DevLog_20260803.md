# Day 8 — SystemUI Development: Buildroot Qt5, systemui Build & Display Debug (2026-08-03)

**Objective:** Bring up the factory SystemUI (Qt5 QML desktop) on our custom
buildroot rootfs: build the Qt5 cross toolchain environment, port and compile
the systemui source (originally Qt 5.6-era), deploy it with auto-start, and
fix the display noise/jitter problems down to the LCD signal level.

---

## 1. Development Environment Setup

### 1.1 Source Code

Copied the entire factory `SourceCode` folder (1.2 GB) from the ATK board
package into `EmbeddedLinuxNewBSP/`, then extracted `systemui.tar.bz2` inside
`09、Qt综合例程源码/`:

```
systemui/        main program (QML + C++)
dashboard/ music/ player/ camera/ ...   23 app projects
server/          SystemUICommonApiServer (Qt Remote Objects)
ui/              deployment tree (src/ + compiled binaries)
build.sh         one-key build script (needs factory Yocto Qt5 SDK)
```

### 1.2 Manual Study (PDF key points)

- UI is **QML + C++**, factory runtime Qt **5.12.9**; the build.sh uses the
  Yocto SDK `fsl-imx-x11 4.1.15-2.1.0` whose Qt is **5.6** — so the source is
  effectively Qt 5.6-era code.
- Deployment: `systemui` binary → `/opt/ui/systemui`; apps →
  `/opt/ui/src/apps/`; displayed apps configured by `apk1~3.cfg`.
- Display plugin is **Linuxfb** (no window manager; tearing is expected).

### 1.3 Planning Method — package list from two-level scan

The buildroot Qt5 package set was derived from **both** dependency layers:

| Layer | Source | Example |
|:---|:---|:---|
| Compile-time | each `.pro` `QT +=` | dashboard: `3d*` → qt53d |
| Runtime | each `.qml` `import` | `QtQuick.Controls 2.12` → qt5quickcontrols2 |

Minimal set for systemui alone: **qt5base (+linuxfb), qt5declarative(+quick),
qt5quickcontrols2, qt5remoteobjects** (server.pri uses `QT += remoteobjects`)
+ `BR2_TOOLCHAIN_BUILDROOT_CXX=y`.

---

## 2. Buildroot Qt5 Build

`qt5.fragment` (CXX + 4 packages) merged with
`support/kconfig/merge_config.sh` (buildroot path, not `scripts/kconfig/`).

### 2.1 Issues

| Issue | Root cause | Fix |
|-------|-----------|-----|
| merge_config.sh not found | buildroot stores it at `support/kconfig/` | use correct path |
| `qt5base: Cannot run target compiler ... g++` | toolchain was built **C-only**; `BR2_TOOLCHAIN_BUILDROOT_CXX` was off → no `g++` exists | add CXX option + `make host-gcc-final-dirclean` + full rebuild (~30 min) |
| `qt5graphicaleffects` needs OpenGL | i.MX6ULL has no GPU | the QML `import QtGraphicalEffects` in systemui is unused → delete import, skip package |

Rootfs v1 verified: `libQt5*.so` (16 libs), `libqlinuxfb.so`, `/dev/fb0`,
GT911 touch, network and audio all intact.

---

## 3. Rootfs Deploy Accident & U-Boot Rescue

### 3.1 The Accident

Deploying rootfs v1 with `mount /dev/mmcblk1p2 /mnt; rm -rf /mnt/*` while the
board was running its own rootfs **deleted `/bin/login` and busybox** → board
bricked (Kernel panic: no working init).

### 3.2 Rescue Path

1. U-Boot rescue boot `init=/bin/sh` failed (busybox itself was deleted).
2. Solution: buildroot `BR2_TARGET_ROOTFS_EXT2` → produce **rootfs.ext2**
   image → copy to USB stick → U-Boot:
   ```
   usb start
   fatload usb 0:1 0x81000000 rootfs.ext2
   mmc write 0x81000000 0x45000 0x40000   # p2 starts at sector 282624 = 0x45000
   boot
   ```
3. **Critical trap:** `mmc write ... 282624` — U-Boot parses numbers as
   **hexadecimal**, so `282624` became decimal 2631204 and the image was
   written to the wrong block. Correct form is `0x45000`.

### 3.3 SSH Maintenance after re-flash

- `authorized_keys` was replaced (factory ed25519 key ≠ our ssh-rsa key).
- dropbear host key changed → `ssh-keygen -R 192.168.1.100`.
- Board has no sftp-server → use **`scp -O`** (legacy protocol).

---

## 4. systemui Build & Deployment

### 4.1 Cross-compile

```
cd systemui/
output/host/bin/qmake systemui.pro
make -j8
```
Only deprecated warnings (`QProcess::start(QString)`, `qrand()`) — no errors.
`QMAKE_POST_LINK` auto-copies the binary to `ui/systemui`.

### 4.2 Rootfs v2 (overlay)

- `ui/` (23 MB) → `board/atk-imx6u/rootfs-overlay/opt/ui`
- `/etc/init.d/S99systemui` starts `QT_QPA_PLATFORM=linuxfb ./systemui &`
- `apk1~3.cfg` emptied (no apps built yet)
- **Missing libs:** systemui failed with
  `libstdc++.so.6: cannot open shared object` — buildroot's gcc-final
  install mechanism was broken (missing `.files-list`). Manually copied
  `libstdc++.so*` + `libatomic.so*` from the toolchain into the overlay.

---

## 5. QML Compatibility Port (Qt 5.6 → 5.15)

| Problem | Fix |
|---------|-----|
| `import QtGraphicalEffects 1.12` (4 files) — unused but GL-only | delete import lines |
| `OpacityMask is not a type` (BottomApp) | remove the OpacityMask block (visual only) |
| `FuzzPanel unavailable` (needs QtGraphicalEffects blur) | remove FuzzPanel block (it was `visible:false` anyway) |
| `read .../ATK/apk1.cfg failed` | cfg dir is `ATK-IMX6U`; board hostname is `ATK` → rename dir to `ATK` |

---

## 6. Images & Fonts

- jpg/png both fail: "Unsupported image format" — qt5base has no
  jpeg/png plugins. **BMP works** (built-in decoder) → converted background
  image to BMP and pre-scaled to native 1024×600 (avoids scaling).
- Fonts: `/usr/lib/fonts` missing → deployed DejaVuSans + **wqy-zenhei**
  (Chinese) → text renders.

---

## 7. Display Noise Debug (the core chapter)

**Symptom:** colored dots (white/yellow) on image lines + text jittering
horizontally.

### 7.1 Test sequence (systematic exclusion)

| Test | Result | Conclusion |
|------|--------|-----------|
| Solid red → fb0 | clean | LCD/fb fine |
| BMP pixel analysis (isolated/yellow dots) | clean | image fine |
| Checkerboard 8px (high-contrast edges) | clean | timing sampling OK at block level |
| **fb0 screenshot captured → viewed on PC** | **clean** | **Qt rendering chain 100% fine** |

→ Since the framebuffer content is perfect but the physical screen shows
dots, the corruption happens **between eLCDIF and the LCD driver IC**:
signal-integrity / PCLK sampling edge issues during rapid data transitions
(crosstalk, ground bounce, slew).

### 7.2 Fixes

| Attempt | Change | Result |
|---------|--------|--------|
| 1. Invert PCLK sampling edge | `pixelclk-active = <0>` → `<1>` | ~50% improvement; **jitter gone** |
| 2. Stronger pad drive + SRE | LCDIF dat/ctrl pinctrl `0x49` → `0x1b0b0` | **fully clean** ✅ |

Note: `0x49` was the manual's SGM3157-recommended low drive; with
`pixelclk-active=<1>` the sampling phase avoids the unstable transition
zone, and `0x1b0b0` (faster slew, stronger drive) sharpens the edges further.

---

## 8. App Deployment & PNG/JPEG Enablement

### 8.1 Building led + clock Apps

Only these two apps were built (per plan). Both compiled **first-try with zero
errors** using the buildroot cross qmake:

```
cd led/ && output/host/bin/qmake led.pro && make -j8    # → led
cd clock/ && output/host/bin/qmake clock.pro && make -j8 # → clock
```

- Both use `client.pri` (`QT += remoteobjects` + `REPC_REPLICA`) — already
  available.
- `QMAKE_POST_LINK` auto-copied the binaries to `ui/src/apps/`.
- Deployment: binaries → `/opt/ui/src/apps/`; icons (appicons/*.png);
  config via cfg files (page1 = LED, page2 = clock).
- Launch mechanism verified: systemui executes
  `/opt/ui/src/apps/<programName>` (from cfg column 3).

### 8.2 Dock Configuration

`apk3.cfg` controls the bottom Dock (BottomApp reads it at startup):

```
appicons/led.png LED led
appicons/clock.png 时钟 clock
```

Dock shows both icons; tapping launches the app (same launch path as the
pages). Swipe left/right switches page 1 ↔ page 2 with the Dock intact.

### 8.3 Black-corner Icon Problem

After converting icons to BMP (no alpha), the rounded-corner icons showed
**four black right-angle corners** — BMP has no alpha channel, so the
transparent area rendered black.

### 8.4 PNG/JPEG Enablement (the proper fix)

Added to the buildroot fragment:

```
BR2_PACKAGE_QT5BASE_PNG=y
BR2_PACKAGE_QT5BASE_JPEG=y
```

Findings during the rebuild:
- **PNG is built INTO QtGui** (qpnghandler, not a plugin) — no `libqpng.so`
  exists; once configured it just works.
- **JPEG is a plugin** (`libqjpeg.so` in `plugins/imageformats/`).
- **Adding a dependency does NOT automatically re-configure qt5base** —
  `make qt5base-dirclean` was required before the new options took effect.
- Rebuilt rootfs (v3), re-flashed via the USB-stick + `mmc write` flow.
- Reverted cfg icons to the original `.png` — rounded corners now transparent,
  background image could also return to jpg if desired.

### 8.5 Fonts into the Overlay

Fonts (DejaVuSans + wqy-zenhei) were added to
`rootfs-overlay/usr/lib/fonts/` so every future rootfs build includes them
(no more manual scp after re-flash).

### 8.6 Touch Interaction Verification (verify round 2)

- Swipe left/right: page 1 (LED) ↔ page 2 (clock) — smooth.
- Tapping LED icon: LED control UI opens ("电筒关").
- Tapping clock icon: clock UI shows current time.
- Tapping Dock icons: apps launch correctly.
- All touch coordinates correct; no crashes.

---

## 9. WiFi Enablement (RTL8188EUS)

### 10.1 Discovery

A **Realtek RTL8188EUS** USB WiFi dongle (0bda:8179) was attached to the USB
hub, but no driver/tools were present (no `wlan0`, no wpa_supplicant).
Kernel already had `CFG80211=y` + `MAC80211=y`.

### 10.2 Kernel Enablement

`wifi.fragment`:

```
CONFIG_RTL8XXXU=y
```

Build pitfalls:
- **merge_config does NOT trigger a rebuild of rtl8xxxu** — had to `make
  drivers/net/wireless/realtek/rtl8xxxu/` manually first.
- Deleting `vmlinux` to force a full re-link caused a **parallel-build race**
  (`drm_gpuva_mgr.o` missing → `ar: Error 123`) — fixed by rebuilding
  `drivers/gpu/drm/ sound/` incrementally, then `make zImage`.

### 10.3 regulatory.db Saga

- `CFG80211_REQUIRE_SIGNED_REGDB=y` **cannot be disabled** (Kconfig
  `default y`), so a signed regulatory.db is mandatory.
- Downloaded the official `regulatory.db` + **`regulatory.db.p7s`** (signature)
  from the wireless-regdb repo.
- **Timing trap:** cfg80211 initializes at ~3.3 s but rootfs (mmcblk1p2)
  mounts at ~3.4 s → placing the files in the rootfs overlay fails to load.
- **Fix (same as SDMA firmware):** embed both files into the kernel with
  `CONFIG_EXTRA_FIRMWARE="imx/sdma/sdma-imx6q.bin regulatory.db regulatory.db.p7s"`
  (files must exist in `EXTRA_FIRMWARE_DIR=/lib/firmware` at build time).

### 10.4 Firmware & Rootfs v4

- `rtl8188eufw.bin.zst` (from host `/lib/firmware/rtlwifi/`) decompressed →
  overlay `/lib/firmware/rtlwifi/rtl8188eufw.bin`.
- buildroot: `BR2_PACKAGE_WPA_SUPPLICANT=y` → rootfs v4, re-flashed.
- Boot log now shows: `RTL8188EU rev D`, `Loading firmware rtlwifi/rtl8188eufw.bin`,
  `Firmware revision 28.0`.

### 10.5 Connection (SSID typo lesson)

- First attempt used a wrong SSID (**DELL357**) — scans showed 10 BSSes but
  none matched, "No suitable network found".
- Correct SSID is **BELL357** (WPA2-PSK): authenticate → associated →
  `LOWER_UP` → DHCP `192.168.2.57/24` → `ping 8.8.8.8` **0% loss**.
- `eth0` (192.168.1.100) untouched; wlan0 is a parallel network.

### 10.6 Auto-connect at Boot

`/etc/init.d/S50wifi` (in the overlay):
1. wait for `/sys/class/net/wlan0` (≤15 s)
2. start `wpa_supplicant -B -i wlan0 -c /etc/wpa_supplicant.conf -D nl80211`
3. wait for `operstate = up` (≤30 s)
4. `udhcpc -i wlan0 -q`

`/etc/wpa_supplicant.conf` (BELL357) also lives in the overlay. Verified by
running the script manually; a reboot check confirms full auto-connect.

---

## 10. Final State

- Rootfs v3 with JPEG/PNG support; systemui auto-starts at boot
  (S99systemui), background image + clock + search + page indicator.
- Apps: led (page 1), clock (page 2), both in the bottom Dock.
- PNG icons with native transparency; fonts on board; display fully clean.
- WiFi (RTL8188EUS): driver + embedded regulatory.db/p7s + firmware +
  wpa_supplicant; auto-connect BELL357 → 192.168.2.57 (S50wifi).

---

## 11. Key Takeaways

1. **U-Boot numbers are hex** — always write `mmc write` block addresses in
   hex (`0x45000`), not decimal.
2. **Never deploy over a running rootfs** — boot a rescue system first
   (ext2 image via USB + `mmc write` is the reliable path).
3. **Buildroot Qt5 needs C++ toolchain** — `BR2_TOOLCHAIN_BUILDROOT_CXX=y`
   plus a forced gcc-final rebuild.
4. **Qt 5.15 has no GL here** — graphicaleffects is unavailable; remove
   unneeded imports/effects instead of enabling GL.
5. **Framebuffer screenshot debugging** — dump fb0, convert to BMP, and
   view on PC to separate software rendering from hardware/signal issues.
6. **Display noise on i.MX6ULL RGB888** — try `pixelclk-active` inversion
   first, then stronger pinctrl (`0x1b0b0`); keep the manual's SGM3157
   drive-strength note in mind.
7. **`scp -O`** for dropbear boards without sftp-server; refresh
   `authorized_keys` and host keys after every rootfs re-flash.
8. **Qt 5.15 PNG is built into QtGui** (no plugin) while **JPEG is a
   plugin** (`libqjpeg.so`) — check the real mechanism before assuming.
9. **New qt5base options need `make qt5base-dirclean`** — adding a
   dependency does not trigger a re-configure.
10. **Rootfs re-flash wipes runtime changes** — fonts/keys/UI tweaks must
    live in the buildroot overlay, not just on the board.
11. **Early-boot firmware must be embedded into the kernel**
    (`CONFIG_EXTRA_FIRMWARE`) — rootfs (mmcblk1p2) mounts too late for
    cfg80211/SDMA to load it (regulatory.db needs its signed `.p7s` too).
12. **`CONFIG_RTL8XXXU` needs a manual rebuild after merge_config** — and a
    full `vmlinux` deletion can trigger parallel-build races (fix the
    failing dirs incrementally).
13. **RTL8188EUS is 2.4 GHz only** — 5 GHz APs are invisible; verify the SSID
    string carefully before debugging the radio.
